name: jeizicorp_cleaning_module
description: Modulo de gestion para auxiliar de limpieza (EPP, quimicos, etiquetas, KPIs)
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1

flutter:
  uses-material-design: true
